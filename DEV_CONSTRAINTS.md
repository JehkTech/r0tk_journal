# Dev Constraints

## AI Execution Rules
- Use backend-only API calls (frontend talks to backend; backend talks to Supabase).
- After each completed feature phase, run the appropriate tests and record results in a report document before continuing.
- If tests are skipped, document the reason and the risk in the report.
- Always split tasks into todos and keep the todo list current.
